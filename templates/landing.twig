<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TicketFlow</title>
  <!-- Use the same Tailwind source as layout.twig -->
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/style.css">
  <style>
    @keyframes spin-slow {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    .animate-spin-slow {
      animation: spin-slow 8s linear infinite;
    }
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }
    .animate-float {
      animation: float 4s ease-in-out infinite;
    }
  </style>
</head>
<body class="min-h-screen bg-gray-50">

  {# ====================== HERO SECTION ====================== #}
  <div class="relative bg-gradient-to-br from-blue-600 to-purple-700 text-white overflow-hidden">

    <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-20 pb-40 text-center space-y-8">
      <h1 class="text-6xl md:text-7xl font-extrabold tracking-tight">TicketFlow</h1>
      <p class="text-xl md:text-2xl text-blue-100 max-w-3xl mx-auto leading-relaxed">
        Streamline your support workflow with powerful ticket management
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center pt-4">
        <a href="login.php"
           class="min-w-[160px] bg-white text-blue-600 px-8 py-3.5 rounded-xl font-bold text-lg hover:bg-blue-50 transition-all duration-200 shadow-lg hover:shadow-xl">
          Login
        </a>
        <a href="signup.php"
           class="min-w-[160px] bg-blue-500 text-white px-8 py-3.5 rounded-xl font-bold text-lg hover:bg-blue-400 transition-all duration-200 shadow-lg hover:shadow-xl border-2 border-blue-400">
          Get Started
        </a>
      </div>
    </div>

    {# Decorative Circles #}
    <div class="absolute top-20 right-20 w-72 h-72 bg-white opacity-10 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-40 left-10 w-56 h-56 bg-purple-400 opacity-20 rounded-full blur-2xl"></div>
    <div class="absolute top-1/3 right-1/4 w-40 h-40 bg-blue-300 opacity-10 rounded-full blur-xl"></div>

    <div class="absolute top-32 left-1/4 w-24 h-24">
      <div class="w-full h-full rounded-full border-4 border-white opacity-20 animate-spin-slow"></div>
    </div>
    <div class="absolute bottom-24 right-1/3 w-16 h-16">
      <div class="w-full h-full rounded-full bg-gradient-to-r from-pink-400 to-purple-500 opacity-20 animate-bounce"></div>
    </div>
    <div class="absolute top-40 right-1/2 w-12 h-12">
      <div class="w-full h-full rounded-full border-2 border-dashed border-white opacity-30 animate-spin"></div>
    </div>
    <div class="absolute bottom-32 left-1/3 w-20 h-20">
      <div class="w-full h-full rounded-full bg-gradient-to-br from-yellow-400 to-orange-500 opacity-20 animate-float"></div>
    </div>

    {# Wave SVG #}
    <div class="absolute bottom-0 left-0 right-0">
      <svg class="w-full h-auto" viewBox="0 0 1440 100" fill="none" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="none">
        <path d="M0,50 C240,100 480,0 720,50 C960,100 1200,0 1440,50 L1440,100 L0,100 Z" fill="#F9FAFB"/>
      </svg>
    </div>
  </div>

  {# ====================== FEATURES SECTION ====================== #}
  <div class="relative -mt-20 z-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-20">
      <div class="grid md:grid-cols-3 gap-8 lg:gap-10">

        {# Feature 1 #}
        <div class="bg-white rounded-2xl p-8 shadow-xl hover:shadow-2xl transition-all duration-300 hover:-translate-y-2 border border-gray-100">
          <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl flex items-center justify-center mb-6 shadow-lg">
            <svg xmlns="http://www.w3.org/2000/svg" class="text-white w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m2 0a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-3 text-gray-900">Easy Ticket Management</h3>
          <p class="text-gray-600 leading-relaxed">
            Create, track, and resolve tickets with an intuitive interface designed for efficiency.
          </p>
        </div>

        {# Feature 2 #}
        <div class="bg-white rounded-2xl p-8 shadow-xl hover:shadow-2xl transition-all duration-300 hover:-translate-y-2 border border-gray-100">
          <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-purple-600 rounded-2xl flex items-center justify-center mb-6 shadow-lg">
            <svg xmlns="http://www.w3.org/2000/svg" class="text-white w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3 3v18h18M3 12h18M9 21V3M15 21V3"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-3 text-gray-900">Real-time Analytics</h3>
          <p class="text-gray-600 leading-relaxed">
            Monitor ticket status and team performance at a glance with powerful insights.
          </p>
        </div>

        {# Feature 3 #}
        <div class="bg-white rounded-2xl p-8 shadow-xl hover:shadow-2xl transition-all duration-300 hover:-translate-y-2 border border-gray-100">
          <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-green-600 rounded-2xl flex items-center justify-center mb-6 shadow-lg">
            <svg xmlns="http://www.w3.org/2000/svg" class="text-white w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-3 text-gray-900">Streamlined Workflow</h3>
          <p class="text-gray-600 leading-relaxed">
            Boost productivity with automated notifications and seamless updates.
          </p>
        </div>

      </div>
    </div>
  </div>

  {# ====================== FOOTER ====================== #}
  <footer class="bg-gray-900 text-white py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <p class="text-gray-400">&copy; 2025 TicketFlow. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
